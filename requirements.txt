numpy>=1.23,<1.27           # 1.26.4 OK, but loosened slightly for compatibility
pandas>=2.2.0,<2.3          # 2.2.3 is safe
scikit-learn>=1.2,<1.3      # 1.2.2 OK; 1.3+ might break SHAP or xgboost
shap>=0.44,<0.45            # 0.44.1 OK
xgboost>=2.0,<2.1           # 2.0.3 OK
requests>=2.31,<2.32        # 2.31.0 OK
yfinance>=0.2.79,<0.3      # 0.2.79 OK



# PyTorch with CUDA 12.4
# --extra-index-url https://download.pytorch.org/whl/cu124
# torch==2.6.0+cu124
## please install the right torch version for your CUDA version; the above is for CUDA 12.4, but you may need a different one
## simply replace 124 with your version, e.g., 121 for CUDA 12.1, or use the CPU-only version if you don't have a compatible GPU

fastparquet>=2024.11.0      # current latest version
pyarrow>=12,<14             # 13.x supports pandas 2.2.x and numpy 1.26
